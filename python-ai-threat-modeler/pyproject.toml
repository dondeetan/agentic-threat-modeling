[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-threat-modeler-mvp"
version = "0.1.0"
description = "FastAPI MVP for automated threat modeling"
requires-python = ">=3.11"
